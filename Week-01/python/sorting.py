pairs  = [("apple",5), ("kiwi",4), ("Banana",6), ("Cherry",6)]
prices = {"apple":1.2, "kiwi":0.8, "cherry":4.0}

# Sort pairs by the second element — using a lambda
# Same — using a named def
# Same — using itemgetter
# Same, descending
# Sort words alphabetically, case-insensitively
# Sort pairs by length descending, then alphabetically as a tiebreak — one sorted call, one key
# Sort prices by value, cheapest first